VITE_BASE=/

# 权限：菜单与按钮由 IAM 控制（见 preferences.ts accessMode: backend）

# 是否开启压缩
VITE_COMPRESS=gzip

# 是否开启 PWA
VITE_PWA=false

# 是否注入全局loading
VITE_INJECT_APP_LOADING=true

# 打包后是否生成dist.zip
VITE_ARCHIVER=true
